/** Autocomplete : local-first SQLite + Mapbox distant (seuil 12 car., auto si local vide). */
export const ENABLE_AUTOCOMPLETE = false;

/**
 * Concept Sourced Intelligence — extraction ancrée + Inbox mémorable.
 * Rollback instantané : passer à `false` (comportement prod pré-feature).
 */
export const SOURCING_V1_ENABLED = true;
