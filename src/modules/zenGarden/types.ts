export type BloomVisualState = 'bloom' | 'wilted' | 'single_task';

/** XP requis pour passer au niveau suivant (affichage & arc). */
export const ZEN_XP_PER_LEVEL = 1000;

/** Plafond des crédits « Projet » affichés (ex. 2/3). */
export const ZEN_PROJECT_CREDITS_MAX = 3;
